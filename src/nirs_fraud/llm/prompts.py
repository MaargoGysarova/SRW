from __future__ import annotations

from ..catalog.taxonomy import scenario_ids, signal_ids


QWEN_JSON_RULES = """
Критически важно:
- Верни только один JSON-объект.
- Не используй markdown, не добавляй ```json.
- Не добавляй пояснения до или после JSON.
- Все ключи и строковые значения должны быть в двойных кавычках.
- Не используй trailing commas.
- Не используй шаблонные значения вроде "temporary_id", "example", "..." или "fraud|suspicious|safe".
""".strip()


SINGLE_LLM_PROMPT = """
Ты классификатор мошеннических диалогов.
Твоя задача: прочитать текст и вернуть JSON:
{
  "label": "fraud|suspicious|safe",
  "fraud_score": 0.0,
  "signals": ["..."],
  "explanation": "краткое объяснение"
}

Классы:
- fraud: явные признаки мошенничества или социальной инженерии
- suspicious: есть тревожные сигналы, но контекст недостаточно однозначен
- safe: признаков мошенничества нет
"""


GENERATOR_PROMPT = """
Ты LLM-генератор синтетических русскоязычных диалогов для исследования мошенничества.

На входе ты получаешь:
- label
- scenario
- required_signals
- optional_signals
- forbidden_signals
- difficulty
- style_notes

Сгенерируй один реалистичный диалог на русском языке и верни JSON:
{
  "id": "exact_request_id",
  "label": "exact_input_label",
  "scenario": "exact_input_scenario",
  "text": "готовый диалог",
  "signals": ["canonical_signal_id"],
  "difficulty": "exact_input_difficulty",
  "source": "synthetic_llm_generated",
  "language": "ru",
  "modality": "text"
}

Правила:
- Диалог должен быть правдоподобным и кратким.
- Диалог должен звучать естественно для русского языка, без канцелярита и англицизмов без необходимости.
- Используй 2-5 реплик, если сценарий не требует другого.
- Если это телефонный сценарий, явно разделяй реплики по ролям: "Оператор:", "Клиент:", "Следователь:" и т.д.
- Не добавляй персональные данные реальных людей.
- Для fraud и suspicious обязательно отражай заявленные сигналы в тексте.
- Для safe не используй мошеннические триггеры без необходимости.
- Не придумывай лишние сигналы, если они не соответствуют сценарию.
- Поле "signals" должно содержать только канонические signal ids.
- Поле "label" должно содержать только одно значение: fraud, suspicious или safe.
- Никогда не перечисляй варианты через "|" или ",".
- Поле "scenario" должно в точности совпадать с входным scenario id, не переводи его и не перефразируй.
- Поле "id" должно в точности совпадать с переданным request_id.
- Если required_signals пустой, верни "signals": [].
- Не используй английские описания сценария вместо scenario id.
"""


AUGMENTATOR_PROMPT = """
Ты LLM-аугментатор датасета.

Твоя задача не просто переписать текст, а создать новый, правдоподобный и полезный вариант для обучения и проверки устойчивости детектора.
Хорошая аугментация:
- сохраняет label, scenario и ключевые сигналы;
- заметно отличается от исходника;
- остаётся естественным русским диалогом;
- не выглядит как механическая замена 1-2 слов;
- делает датасет разнообразнее, а не беднее.

На входе дан исходный диалог и тип аугментации:
- paraphrase
- subtle
- asr_noise
- scenario_variation

Верни JSON:
{
  "id": "exact_request_id",
  "base_id": "exact_base_id",
  "augmentation_type": "exact_augmentation_type",
  "label": "exact_input_label",
  "text": "готовый аугментированный диалог",
  "preserved_signals": ["canonical_signal_id"],
  "notes": "краткое описание изменений"
}

Требования:
- Сохраняй исходную метку.
- Сохраняй общий смысл и тип сценария.
- Не превращай safe в fraud и наоборот без явной причины.
- Для asr_noise вноси правдоподобные ошибки распознавания речи, а не случайные опечатки.
- Для paraphrase меняй формулировки, структуру реплик и локальную аргументацию, но не основную семантику.
- Для subtle делай признаки менее очевидными, но всё ещё различимыми для аналитика и однозначно мошенническими для эксперта.
- Для scenario_variation меняй только обрамляющий контекст, а не целевой класс.
- Поле "base_id" должно в точности совпадать с id исходного примера.
- Поле "label" должно в точности совпадать с исходной меткой.
- Поле "augmentation_type" должно в точности совпадать с переданным типом аугментации.
- Поле "preserved_signals" должно содержать только канонические signal ids.
- Не используй шаблонные значения вроде temporary_aug_id или fraud|suspicious|safe.
- Если вариант получился слишком близким к исходнику, перепиши его глубже.
"""


VALIDATOR_PROMPT = """
Ты LLM-валидатор синтетических примеров.

Проверь пример по чек-листу:
1. Соответствует ли текст метке?
2. Есть ли заявленные сигналы?
3. Нет ли реальных персональных данных?
4. Нет ли логических противоречий сценария?
5. Годится ли пример для включения в датасет?

Верни JSON:
{
  "accept": true,
  "label_consistency": "ok|warning|fail",
  "signal_consistency": "ok|warning|fail",
  "privacy_check": "ok|warning|fail",
  "issues": ["..."],
  "recommended_fixes": ["..."]
}

Если пример невалиден, не переписывай его целиком, а укажи проблемы в `issues` и `recommended_fixes`.
"""


CHECKLIST_PROMPT = """
Проанализируй диалог по чек-листу сигналов мошенничества.

Сигналы:
""" + ", ".join(signal_ids()) + """

Сначала заполни найденные сигналы, затем вычисли fraud_score и класс.
"""


SELF_CHECK_PROMPT = """
Шаг 1. Дай первичную классификацию и объяснение.
Шаг 2. Попробуй опровергнуть собственный вывод:
- какие сигналы могли быть ложными?
- достаточно ли контекста?
- не является ли диалог обычным бытовым разговором?
Шаг 3. Выдай финальный JSON с обновленным классом.
"""


ENSEMBLE_PROMPT = """
Смоделируй три независимых мнения:
1. Осторожный аналитик.
2. Эксперт по банковому фроду.
3. Скептичный reviewer, который снижает false positives.

Пусть каждый даст label и короткое объяснение.
Затем верни итоговое голосование и агрегированный список сигналов.
"""


CLASSIFICATION_JSON_RULES = """
Критически важно:
- Верни только один JSON-объект.
- Не используй markdown и code fences.
- Не добавляй пояснения вне JSON.
- Используй схему:
{
  "label": "fraud|suspicious|safe",
  "fraud_score": 0.0,
  "signals": ["canonical_signal_id"],
  "explanation": "краткое объяснение"
}
""".strip()


GENERATOR_OUTPUT_EXAMPLE = """
Пример корректного ответа по формату:
{
  "id": "seed_001_gen_01",
  "label": "fraud",
  "scenario": "bank_security_call",
  "text": "Оператор: Здравствуйте, это служба безопасности банка. По вашей карте пытаются провести перевод. Клиент: Я ничего не подтверждал. Оператор: Тогда срочно назовите код из СМС, чтобы отменить операцию.",
  "signals": ["bank_impersonation", "urgency", "sms_code_request"],
  "difficulty": "easy",
  "source": "synthetic_llm_generated",
  "language": "ru",
  "modality": "text"
}

Это только пример формата ответа.
Не копируй этот текст буквально.
""".strip()


AUGMENTATOR_OUTPUT_EXAMPLE = """
Пример корректного ответа по формату:
{
  "id": "seed_001_gen_01_paraphrase",
  "base_id": "seed_001_gen_01",
  "augmentation_type": "paraphrase",
  "label": "fraud",
  "text": "Оператор: Добрый день, это служба безопасности банка. По вашей карте пытаются провести списание. Клиент: Я ничего не подтверждал. Оператор: Тогда срочно сообщите код из СМС, чтобы остановить операцию.",
  "preserved_signals": ["bank_impersonation", "urgency", "sms_code_request"],
  "notes": "Переформулировал фразы, сохранив смысл и сигналы."
}

Это только пример формата ответа.
Не копируй этот текст буквально.
""".strip()


SUBTLE_AUGMENTATION_EXAMPLE = """
Пример subtle-аугментации:
Исходник:
Оператор: Здравствуйте, это служба безопасности банка. У нас есть сообщение о попытке перевода средств с вашего счета. Клиент: Я ничего не делал. Оператор: Пожалуйста, подтвердите операцию, назвав код из СМС, который мы вам отправили.

Плохой subtle-вариант:
Оператор: Здравствуйте, это служба безопасности банка. Мы получили сообщение о попытке перевода средств с вашего счета. Клиент: Я ничего не делал. Оператор: Пожалуйста, подтвердите операцию, назвав код из СМС, который мы вам отправили.

Почему это плохо:
- изменено слишком мало;
- мошенническая формула осталась почти такой же;
- это поверхностный рерайт, а не новая полезная вариация.

Хороший subtle-вариант:
Оператор: Добрый день, из банка беспокоят по поводу подозрительного списания. Клиент: Я ничего не переводил. Оператор: Чтобы мы успели остановить операцию, продиктуйте цифры из уведомления, которое сейчас поступит.

Почему это subtle:
- смысл сохранился;
- мошенничество осталось fraud;
- признаки стали менее прямолинейными;
- текст не совпадает с исходником дословно.
""".strip()


ASR_NOISE_AUGMENTATION_EXAMPLE = """
Пример asr_noise-аугментации:
Исходник:
Оператор: Здравствуйте, это служба безопасности банка. Пожалуйста, подтвердите операцию, назвав код из СМС, который мы вам отправили.

Плохой asr_noise-вариант:
Оператор: Здравствуйте, это служба безопасности банка. Пожалуйста, подтвердите операцию, назвав код из СМС, который мы вам отправили.

Почему это плохо:
- исходник почти не изменился;
- нет заметного эффекта ASR;
- такая аугментация не добавляет новой сложности.

Хороший asr_noise-вариант:
Оператор: Здравствуйте, это служба безопасности банка. Пожалуйста, потвердите операцию, назвав код из эсэмэс, который мы вам отправили.

Другой хороший asr_noise-вариант:
Оператор: Здравствуйте, это служба безопасности банка. Пожалуйста, подтвердите операцию, назвав кот из смс, который мы вам отправили.

Почему это хороший asr_noise:
- смысл сохранён;
- появились ошибки, похожие на автоматическое распознавание речи;
- текст не совпадает с исходником дословно;
- искажения умеренные: текст остаётся понятным человеку.
""".strip()


SCENARIO_SPECIFIC_RULES = {
    "bank_security_call": [
        "Мошенник должен сам инициировать тревогу о переводе, списании или подозрительной операции.",
        "В тексте должна быть явная просьба назвать код из СМС или подтвердить действие немедленно.",
    ],
    "fake_loan_application": [
        "Это не должен быть легитимный диалог, где клиент сам спокойно оформляет кредит.",
        "Нужна ложная тревога про уже оформленный кредит, заявку на кредит или срочную отмену кредита.",
    ],
    "safe_account_transfer": [
        "В тексте обязательно должна быть явная формулировка про безопасный, резервный или защищённый счёт.",
        "Это должен быть именно мошеннический перевод средств, а не нейтральная банковская консультация.",
    ],
    "fake_police_call": [
        "Звонящий должен представляться следователем, полицией, ФСБ или другим силовым органом.",
        "В тексте нужна секретность, давление или запрет рассказывать кому-либо.",
    ],
    "relative_emergency": [
        "Должен быть сценарий беды родственника, аварии, уголовной истории или срочного выкупа/перевода.",
        "Текст должен звучать эмоционально и давить на срочность.",
        "Это не должен быть обычный семейный разговор про самочувствие, поездку или бытовые проблемы.",
        "В fraud-варианте должен быть хотя бы один явный мошеннический ход: просьба срочно перевести деньги, передача трубки якобы следователю, запрет кому-либо звонить или требование сохранить всё в тайне.",
    ],
    "fake_support_app_install": [
        "В тексте должна быть прямая просьба установить приложение, показать экран или дать удалённый доступ.",
    ],
    "government_service_impersonation": [
        "Текст должен явно ссылаться на Госуслуги, госорган, подтверждение аккаунта или защиту профиля.",
    ],
    "investment_scam": [
        "Текст должен обещать быстрый доход, ограниченное предложение или эксклюзивную инвестиционную возможность.",
    ],
    "fake_delivery": [
        "Это должен быть пограничный сценарий доставки, курьера или заказа с нестандартным подтверждением.",
    ],
    "phone_number_extension": [
        "Текст должен говорить о продлении номера, SIM-карты или срочном подтверждении обслуживания.",
    ],
    "voting_link_phishing": [
        "Текст должен содержать просьбу перейти по ссылке под видом голосования или конкурса.",
    ],
    "marketplace_refund": [
        "Текст должен содержать нестандартный возврат денег через ссылку, форму или ввод реквизитов вне обычной процедуры.",
    ],
    "family_logistics": [
        "Это должен быть обычный бытовой диалог без кодов, ссылок, паролей и запугивания.",
    ],
    "doctor_appointment": [
        "Это должно быть нейтральное напоминание о записи или подтверждение визита без подозрительных запросов.",
    ],
    "bank_office_info": [
        "Это должен быть безопасный информационный диалог об отделении, графике или услуге без кодов и паролей.",
    ],
    "it_support_internal": [
        "Это должен быть легитимный внутренний IT-диалог без удалённого доступа, кодов из СМС и передачи паролей.",
    ],
}


def format_generator_prompt(seed_brief: dict, request_id: str) -> str:
    scenario_specific_rules = SCENARIO_SPECIFIC_RULES.get(seed_brief["scenario"], [])
    scenario_specific_block = ""
    if scenario_specific_rules:
        scenario_specific_block = (
            "Дополнительные правила именно для этого сценария:\n"
            + "\n".join(f"- {rule}" for rule in scenario_specific_rules)
            + "\n\n"
        )
    return (
        GENERATOR_PROMPT.strip()
        + "\n\n"
        + QWEN_JSON_RULES
        + "\n\n"
        + GENERATOR_OUTPUT_EXAMPLE
        + "\n\n"
        + "Разрешённые scenario ids:\n"
        + ", ".join(scenario_ids())
        + "\n\n"
        + "Разрешённые signal ids:\n"
        + ", ".join(signal_ids())
        + "\n\n"
        + scenario_specific_block
        + "Точные значения, которые нужно вернуть без изменений:\n"
        + f'- id: "{request_id}"\n'
        + f'- label: "{seed_brief["label"]}"\n'
        + f'- scenario: "{seed_brief["scenario"]}"\n'
        + f'- difficulty: "{seed_brief["difficulty"]}"\n'
        + '- source: "synthetic_llm_generated"\n'
        + '- language: "ru"\n'
        + '- modality: "text"\n'
        + "\n"
        + "Входные параметры сценария:\n"
        + f"- brief_id: {seed_brief['brief_id']}\n"
        + f"- target_count: {seed_brief['target_count']}\n"
        + f"- required_signals: {seed_brief['required_signals']}\n"
        + f"- optional_signals: {seed_brief['optional_signals']}\n"
        + f"- forbidden_signals: {seed_brief['forbidden_signals']}\n"
        + f"- style_notes: {seed_brief['style_notes']}\n"
        + "\n"
        + "Сгенерируй один новый пример, совместимый с этой спецификацией.\n"
        + "Проверь перед ответом:\n"
        + "- что `id` совпадает с request_id символ в символ;\n"
        + "- что `label` равен ровно одному классу без перечисления вариантов;\n"
        + "- что `scenario` совпадает с входным scenario id символ в символ;\n"
        + "- что `scenario` взят только из списка разрешённых scenario ids;\n"
        + "- что `signals` содержит только ids из списка разрешённых signal ids;\n"
        + "- что `signals` содержит все required_signals;\n"
        + "- что `signals` не содержит forbidden_signals;\n"
        + "- что если сигнал не входит в разрешённый список, ты не используешь его вообще;\n"
        + "- что в fraud-примерах есть явное вредоносное действие или социальная инженерия, а не нейтральный сервисный разговор;\n"
        + "- что в safe-примерах нет кодов из СМС, ссылок, паролей, установки приложений и перевода на безопасный счёт;\n"
        + "- что ты не вставляешь реальные телефоны, email, номера карт, паспортные данные или другие персональные данные;\n"
        + "- что `text` не пустой и выглядит как живой русский диалог;\n"
        + "- что ты не использовал шаблонные значения вроде temporary_id, example или fraud|suspicious|safe."
    )


def format_augmentator_prompt(record: dict, augmentation_type: str) -> str:
    augmentation_rules = {
        "paraphrase": [
            "Обязательно замени формулировки минимум в двух местах.",
            "Не копируй исходные предложения дословно.",
        ],
        "subtle": [
            "Сделай мошеннические признаки менее прямыми и менее кричащими.",
            "Замени хотя бы две ключевые формулировки на более завуалированные.",
            "Нельзя возвращать исходный текст или почти дословную копию.",
        ],
        "asr_noise": [
            "Сохрани смысл, но внеси правдоподобные ошибки распознавания речи.",
            "Ошибки должны быть умеренными, чтобы текст оставался читаемым.",
            "Обязательно измени минимум две текстовые точки: слово, словоформу, слитность, пунктуацию или запись аббревиатуры.",
            "Нельзя возвращать исходный текст или почти дословную копию.",
            "Допустимы типичные ASR-искажения вроде: «потвердите», «эсэмэс», «гос услуги», «кридит», «кот из смс».",
        ],
        "scenario_variation": [
            "Сохрани класс и общий смысл, но измени бытовой контекст.",
            "Новый текст не должен совпадать с исходником дословно.",
        ],
    }
    rule_block = "\n".join(f"- {rule}" for rule in augmentation_rules.get(augmentation_type, []))
    return (
        AUGMENTATOR_PROMPT.strip()
        + "\n\n"
        + QWEN_JSON_RULES
        + "\n\n"
        + AUGMENTATOR_OUTPUT_EXAMPLE
        + "\n\n"
        + SUBTLE_AUGMENTATION_EXAMPLE
        + "\n\n"
        + ASR_NOISE_AUGMENTATION_EXAMPLE
        + "\n\n"
        + "Разрешённые signal ids:\n"
        + ", ".join(signal_ids())
        + "\n\n"
        + "Точные значения, которые нужно вернуть без изменений:\n"
        + f'- id: "{record["id"]}_{augmentation_type}"\n'
        + f'- base_id: "{record["id"]}"\n'
        + f'- augmentation_type: "{augmentation_type}"\n'
        + f'- label: "{record["label"]}"\n'
        + "\n"
        + "Исходный пример:\n"
        + f"{record}\n\n"
        + f"Тип аугментации: {augmentation_type}\n"
        + "Дополнительные правила для этого типа аугментации:\n"
        + rule_block
        + "\n\n"
        + "Верни один JSON-объект без дополнительных пояснений.\n"
        + "Проверь перед ответом:\n"
        + "- что `label` сохранён;\n"
        + "- что `base_id` указывает на исходный пример;\n"
        + "- что `augmentation_type` совпадает с переданным типом;\n"
        + "- что `preserved_signals` содержит только ids из списка разрешённых signal ids;\n"
        + "- что `text` не дублирует исходник буквально;\n"
        + "- что в paraphrase, subtle и scenario_variation текст заметно отличается от исходника хотя бы по двум фразам;\n"
        + "- что в asr_noise появились заметные, но умеренные ASR-подобные искажения минимум в двух местах;\n"
        + "- что ты не вставил реальные телефоны, email, номера карт или другие персональные данные."
    )


def format_validator_prompt(record: dict) -> str:
    return (
        VALIDATOR_PROMPT.strip()
        + "\n\n"
        + QWEN_JSON_RULES
        + "\n\n"
        + "Проверяемый пример:\n"
        + f"{record}\n\n"
        + "Верни один JSON-объект без дополнительных пояснений.\n"
        + "Используй строгие оценки:\n"
        + "- `ok`, если всё соответствует;\n"
        + "- `warning`, если пример пограничный, но потенциально пригоден;\n"
        + "- `fail`, если пример не должен попадать в датасет."
    )


def format_classification_prompt(text: str, mode: str) -> str:
    prompt_map = {
        "single_llm": SINGLE_LLM_PROMPT,
        "llm_checklist": CHECKLIST_PROMPT,
        "llm_self_check": SELF_CHECK_PROMPT,
        "llm_ensemble": ENSEMBLE_PROMPT,
    }
    if mode not in prompt_map:
        raise ValueError(f"Unsupported classification mode: {mode}")
    return (
        prompt_map[mode].strip()
        + "\n\n"
        + CLASSIFICATION_JSON_RULES
        + "\n\n"
        + "Диалог:\n"
        + text.strip()
        + "\n\n"
        + "Верни только один JSON-объект."
    )
